// forof loop is a control statement in javascript which returns the elements of the array.
//example:
let myfriends=['eshika','nivedita','sweety','koushik'];
for (let elements of myfriends)
{
    console.log(elements)
}